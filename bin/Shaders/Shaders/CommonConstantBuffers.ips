#include "CommonStructures.ifx"

cbuffer MaterialConstants : register(b0, space1)
{
    float4 ambient;
    float4 diffuse;
    float4 specular;
    float4 emissive;
    float shininess;
};


#ifdef LIGHTING
cbuffer LightParameters: register(b1, space1)
{
    //float3 eyePosition;
    LightConstants m_lights[8];
    float3 camera_position;
}
#endif